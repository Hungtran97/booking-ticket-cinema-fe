import React from 'react';
import PropTypes from 'prop-types';
SideBar.propTypes = {
    
};

function SideBar(props) {
    return (
        <div>
            
        </div>
    );
}

export default SideBar;